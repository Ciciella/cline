import { AiCodeAuthConfig } from './types';

export const aicodeAuthConfig: AiCodeAuthConfig = {
    authServerUrl: 'http://localhost:3000/api',
    clientId: 'aicode-client',
    redirectUri: 'http://localhost:3000/callback'
}; 